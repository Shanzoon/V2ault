# ============================================
# V2ault 环境变量配置示例
# 复制此文件为 .env.local 并根据实际情况修改
# ============================================

# 数据库文件路径
# 默认值: ./images.db (项目根目录)
# 示例 (WSL): /mnt/d/data/images.db
# 示例 (Windows): D:\data\images.db (会自动转换为 WSL 路径)
IMAGE_DB_PATH=./images.db

# 缩略图缓存目录
# 默认值: ../V2ault_cache (项目上一层目录)
# 建议放在 WSL 文件系统内以获得最佳性能
CACHE_DIR=../V2ault_cache

# 图片根目录列表（多个目录用分号分隔）
# 扫描脚本会遍历这些目录下的所有图片
# 示例 (WSL 路径):
IMAGE_ROOTS=/mnt/d/desktop/images;/mnt/h/Development/photos;/mnt/f/ComfyUI/output

# 示例 (Windows 路径，会自动转换):
# IMAGE_ROOTS=D:\desktop\images;H:\Development\photos
